import { useState } from "react"
import { Form } from "react-bootstrap";

export const ComboGenerico = (obj: Object) => {
    const [seleccion, setSeleccion] = useState();

    return (
        Form.Select
    )
}